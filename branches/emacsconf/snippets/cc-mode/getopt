# -*- mode: snippet -*-
#name: getopt
#key: getopt
# --
int opt;
while ( (opt = getopt(argc, argv, "${1:f}:")) != -1 ) {
    switch (opt) {
    case '$1':
        ${2:$1} = optarg;
        break;
    }
}